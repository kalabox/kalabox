'use strict';

var App = require('../lib/app.js'),
  expect = require('chai').expect,
  path = require('path'),
  sinon = require('sinon');

describe('app.js', function () {

  describe('#Constructor()', function () {
    
    var manager_api = {

    },
    appPath = path.resolve('./myapppath/');

    it('Should set the correct properties.', function () {

      // @todo: add tests

    });

  });

});
